<h1>Topics</h1>
<div class="row">
  <div class="col-md-8">

    <!-- loop over each topic in @topics -->
    <% @topics.each do |topic| %>
  <%= render @topics %>
    <% end %>
  </div>
  <% if user_is_authorized_for_topics? %>
  <div class="col-md-4">


<!-- create link to new topic -->

    <%= link_to "New Topic", new_topic_path, class: 'btn btn-success' %>
  </div>
  <% end %>
    </div>
