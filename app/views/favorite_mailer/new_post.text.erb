You are now following the post you created at "<%= @post.title %>"
<%= "=" * (@post.title.length + 12) %>

By: <%= @post.user.name %>
<%= @post.body %>

visit online at: <%= topic_post_url(@post.topic, @post, anchor: "post#{@post.id}",  only_path: true) %>
